# Developer tools for manage web projects
## How install it?
```bash
cd /path/to/applications/
git clone git@github.com:hittten/dev-web-tools.git
composer install --no-dev --optimize-autoloader
```

## Usage
```bash
devtools --help #From any path
```
